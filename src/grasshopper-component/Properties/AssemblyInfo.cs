using System.Runtime.InteropServices;

// The GH_Component's ComponentGuid (declared in HelloGhComponent.cs)
// is separate — that identifies the specific component on a canvas.
// This assembly Guid identifies the .gha as a whole; both must be
// stable across builds. If you fork this sample, generate a fresh
// value once with `uuidgen` and leave it alone.
[assembly: Guid("2dc421d2-6402-439c-a222-80dfb283df51")]
